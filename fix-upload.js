const fs = require('fs');

let code = fs.readFileSync('src/component/learnobject.js', 'utf8');

// The goal: replace UploadShoe Box with a container layout.
// I will not do a script, I'll just formulate a targeted regex replacement.
